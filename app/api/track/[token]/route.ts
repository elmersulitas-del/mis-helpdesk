import { NextResponse } from 'next/server';import { getSupabaseAdmin } from '@/lib/supabase-admin';
export async function GET(_:Request,{params}:{params:Promise<{token:string}>}){const {token}=await params;const {data,error}=await getSupabaseAdmin().from('tickets').select('ticket_number,reporter_name,department,location,category,subject,description,priority,status,assigned_to,resolution_notes,created_at,accepted_at,started_at,resolved_at,updated_at').eq('public_token',token).single();if(error||!data)return NextResponse.json({error:'Ticket not found.'},{status:404});return NextResponse.json(data)}
